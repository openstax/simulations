define(function (require) {

    'use strict';

    var _ = require('underscore');

    var Constants = require('constants');

    // TODO: See if I can find a CSV parser on npm

    /**
     * 
     */
    var Grid = function() {
        
    };

    /**
     * Instance functions/properties
     */
    _.extend(Grid.prototype, {

        /**
         * 
         */
        

    });

    return Grid;
});
