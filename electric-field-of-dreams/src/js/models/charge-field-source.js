define(function (require) {

    'use strict';

    var _ = require('underscore');

    var ChargeFieldSource = function() {};

    /**
     * Instance functions/properties
     */
    _.extend(ChargeFieldSource.prototype, {

        
    });

    return ChargeFieldSource;
});
